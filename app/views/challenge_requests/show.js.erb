<% if @challenge_request.complete? %>
	$("#challenge-<%= @challenge_request.id %>").addClass("complete");
<% else %>
	$("#challenge-<%= @challenge_request.id %>").removeClass("complete");
<% end %>

$(".token-count").text("<%= pluralize(current_user.tokens, "Token") %>");
